{
  "name": "gatsby-theme-early-bird-workspace",
  "private": true,
  "license": "ISC",
  "scripts": {
    "test": "npm run test -w=gatsby-theme-early-bird",
    "develop": "npm run develop -w=example",
    "build": "npm run build -w=example"
  },
  "workspaces": [
    "gatsby-theme-early-bird",
    "example"
  ]
}
